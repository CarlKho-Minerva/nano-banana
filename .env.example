# =============================================================================
# NanoBanana Environment Variables
# =============================================================================
# 
# INSTRUCTIONS:
# 1. Copy this file to '.env' in the root directory
# 2. Fill in your actual values (never commit .env to git!)
# 3. For production, set these in your hosting platform's environment variables
#
# =============================================================================

# -----------------------------------------------------------------------------
# STRIPE CONFIGURATION
# -----------------------------------------------------------------------------
# Get these from: https://dashboard.stripe.com/apikeys
# 
# For development: Use test keys (pk_test_... and sk_test_...)
# For production: Use live keys (pk_live_... and sk_live_...)

# Frontend - Stripe Publishable Key (safe to expose in browser)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Backend - Stripe Secret Key (NEVER expose in frontend!)
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Stripe Webhook Secret (get from webhook settings)
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# -----------------------------------------------------------------------------
# GOOGLE AI CONFIGURATION  
# -----------------------------------------------------------------------------
# Get from: https://aistudio.google.com/app/apikey
# SECURITY: Only backend gets the API key - never expose in frontend!

# Backend - Gemini API Key (SECURE - server only)
GEMINI_API_KEY=your_gemini_api_key_here

# -----------------------------------------------------------------------------
# APPLICATION URLS
# -----------------------------------------------------------------------------
# Development URLs (default)
VITE_BACKEND_URL=http://localhost:3001
FRONTEND_URL=http://localhost:5173

# Production URLs (update these for deployment)
# VITE_BACKEND_URL=https://your-backend.onrender.com
# FRONTEND_URL=https://your-app.vercel.app

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------------------------------------------
# Port for backend server (optional, defaults to 3001)
PORT=3001

# Node environment
NODE_ENV=development